spring:
  application:
    name: gatewayserver
  cloud:
    config:
      uri: http://localhost:8082
      fail-fast: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/eureka